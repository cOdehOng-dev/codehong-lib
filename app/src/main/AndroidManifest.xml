<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE"/>
    <application
        android:name=".sample.SampleApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Codehonglib"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Codehonglib">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".sample.layout.fadeanimheader.FadeAnimHeaderLayoutActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".sample.textfield.SampleTextFieldActivity"/>
        <activity android:name=".sample.button.SampleTextButtonActivity"/>
        <activity android:name=".sample.text.SampleTextActivity"/>
        <activity android:name=".sample.layout.slide.SampleSlideLayoutActivity"/>
        <activity android:name=".sample.calendar.SampleCalendarActivity1"/>
        <activity android:name=".sample.header.SampleHeaderActivity"/>
        <activity android:name=".sample.pager.SampleHorizontalPagerActivity"/>
        <activity android:name=".sample.text.SampleBadgeTextActivity"/>
    </application>

</manifest>